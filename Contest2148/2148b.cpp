#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main () {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int tc; cin >> tc;

  while (tc--) {
    ll n,m,x,y;
    cin >> n >> m >> x >> y;

    for (int i = 0; i < n; i++) {
      ll opa;
      cin >> opa;
    }

    for (int i = 0; i < m; i++) {
      ll opaa;
      cin >> opaa;
    }

    cout << n + m << endl;
  }
}